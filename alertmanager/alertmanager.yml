global:
  resolve_timeout: 5m

route:
  receiver: discord
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2m

receivers:
  - name: discord
    webhook_configs:
      - url: 'http://alertmanager-discord:9094'
